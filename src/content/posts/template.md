---
title: Template
description: Copy this file, change the frontmatter, and set draft to false.
date: 2026-01-01
draft: true
---

Drafts are filtered out of both the index and the route table, so this file never
reaches the built site. It exists so the collection is never empty, which keeps the
build output clean, and so the frontmatter shape is documented where you would look
for it.

Set `draft: false` on a real post and the Writing section appears on the homepage
on its own.
